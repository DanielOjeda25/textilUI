# Perf Report — 2025-11-28

- Entorno: dev server (Vite), ejecuciones headless en CI no permiten medir FPS/CPU del navegador.
- scheduleDraw: intervalo fijo ~33ms → ~30 FPS máximo.
- Redraws durante transform/drag: controlados por `scheduleDraw` y `batchDraw`; no se observaron ráfagas por encima del límite teórico.
- Recomendación: si en QA real se ven >40 redraws/s, aumentar `interval` a 40ms o usar `requestAnimationFrame` con guardas.
- Escenario 50 capas raster: pendiente de medir en navegador (Chrome Performance). Incluir contadores por segundo y registrar CPU/FPS.

